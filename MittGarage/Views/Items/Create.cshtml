@model MittGarage.Models.GarageItem

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>GarageItem</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.regNR)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.regNR)
            @Html.ValidationMessageFor(model => model.regNR)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Owner)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Owner)
            @Html.ValidationMessageFor(model => model.Owner)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Inchecked)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Inchecked)
            @Html.ValidationMessageFor(model => model.Inchecked)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
